kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: fedora-php-s2i-fpm-build
  namespace: fedora-php-s2i-builds
  labels:
    name: fedora-php-s2i-fpm-build
spec:
  output:
    to:
      kind: DockerImage
      name: 'quay.io/cuppett/fedora-php-s2i-fpm:32'
    pushSecret:
      name: quayio
  resources: {}
  successfulBuildsHistoryLimit: 5
  failedBuildsHistoryLimit: 5
  strategy:
    type: Docker
    dockerStrategy:
      from:
        kind: ImageStreamTag
        namespace: fedora-php-s2i-builds
        name: 'fedora-php-s2i-base:32'
  postCommit: {}
  source:
    type: Git
    git:
      uri: 'https://github.com/cuppett/fedora-php-s2i'
    contextDir: fpm
  triggers:
    - type: ImageChange
      imageChange: {}
    - type: ConfigChange
  runPolicy: Serial